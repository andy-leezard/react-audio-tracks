---
"react-audio-tracks": patch
---

New track method: `isPlaying()` and `pauseTrack()` which is a shortcut of `getState().isPlaying` and `togglePlay(false)`. Also clarified the ts docs for `togglePlay()` method.
